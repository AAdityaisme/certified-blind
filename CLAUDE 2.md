# Research Paper (fellowship artifact — actively evolving, no locked title)

Two-domain framing: LLM routing + irreversible satellite EO triage; frame currently leans satellite/triage. This repo is the source of truth — memory and vault cards may lag it.

## Map
- `experiments/` + `experiment_log.md` — runs (Track A ran; T1/T2/T3 reviewed; C2/C3 dropped as audit artifacts)
- `_superseded/` — dead ends, don't resurrect without reason
- `Makefile` — run entry points; check before inventing commands

## Rules
- Scope is LOCKED to writeup (decided 2026-07: no poison-fraction hardening pass, no second domain). Push back if a session starts expanding scope.
- Every claim in the paper needs a run/figure behind it — no aspirational results.
- Writing: research-paper-writing skill for structure; humanizer NOT used here (academic register).
- Purpose: fellowship research artifact (MATS / Anthropic Fellows). Recommender gap is separate — don't conflate.
- Subagents: sonnet; opus only for gnarly analysis.
